using System;

namespace DataStructures
{
    class Program
    {
        static void Main(string[] args)
        {
            //read data with I/O from file

            object[] data = new object[1]; //create new array of data, size of collection

            Array arrayStructure = new Array(data); //store the data in an array


        }
    }
}
